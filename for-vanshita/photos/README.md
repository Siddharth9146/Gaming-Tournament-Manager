# Drop your photos here

Save the 5 photos you shared into this folder with these exact filenames:

| Filename     | Caption it pairs with        | The photo                                    |
| ------------ | ---------------------------- | -------------------------------------------- |
| `photo1.jpg` | "Side by side, always"       | The selfie of you both in front of the yellow-text blackboard |
| `photo2.jpg` | "You, lighting up the night" | Vanshita solo on the balcony at night, city lights behind |
| `photo3.jpg` | "Quiet afternoons with you"  | The outdoor selfie under the shelter, daytime |
| `photo4.jpg` | "Home is wherever you are"   | The cozy bed selfie with the duck plushie & heart blanket |
| `photo5.jpg` | "Colour, chaos, and you"     | The Holi photo with green colour & heart doodles |

**Filenames matter** — they must be exactly `photo1.jpg` through `photo5.jpg`
(lowercase, `.jpg` extension). If your originals are `.jpeg`, `.png`, or `.heic`,
either convert them or update the `src=` paths in `../index.html`.

If a photo is missing, the gallery falls back gracefully to a soft pink card
with an emoji, so the page still looks pretty.
